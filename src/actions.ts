"use server"

export async function addToCart(id: string | number) {
    // TODO: Perform API CALL to ecommerce server
    return Promise.resolve({id})
}

export async function removeFromCart(id: string | number) {
    // TODO: Perform API CALL to ecommerce server
    return Promise.resolve({id})
}

export async function checkout() {
    // TODO: Perform API CALL to ecommerce server and start checkout flow
    return Promise.resolve({})
}